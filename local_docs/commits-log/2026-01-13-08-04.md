# Installation Tip Simplification Commit

## Git commands (execute in order):

```bash
# Stage all changes
git add -A

# Create signed commit
git commit -S -m "docs(install): simplify MCP server tip with links to both transport options"
```

## Changes Summary

- Replaced manual serve instructions with client auto-spawn guidance
- Added links to both MCP Persistent and MCP Network pages
