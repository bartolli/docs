# MCP Server Lifecycle Notes Commit

## Git commands (execute in order):

```bash
# Stage all changes
git add -A

# Create signed commit
git commit -S -m "docs(mcp): add server lifecycle notes for stdio vs HTTP"
```

## Changes Summary

- Added note to mcp-persistent clarifying client auto-spawns stdio server
- Added note to mcp-network clarifying HTTP requires manual server start
