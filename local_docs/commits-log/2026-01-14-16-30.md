# JSON Schema Documentation Commit

## Git commands (execute in order):

```bash
# Stage all changes (excluding reviews/ working directory)
git add docs.json reference/cli.mdx reference/json-schema.mdx workflows/piping.mdx schema/

# Create signed commit
git commit -S -m "docs(reference): add JSON schema documentation and envelope spec

- add reference/json-schema.mdx with envelope structure, fields, data shapes
- add schema/envelope.schema.json for tooling integration
- document --fields flag for filtering JSON output
- update exit codes: 0=success, 1=not_found, 2=error
- expand piping.mdx with envelope examples and error responses"
```

## Changes Summary

- New JSON schema reference page with envelope structure documentation
- Formal JSON Schema file at schema/envelope.schema.json
- CLI reference updated with --fields flag and exit code semantics
- Piping workflow expanded with envelope examples, field filtering, error handling

## Notes

- `reviews/` directory excluded (working files, not documentation)
- Exit codes aligned with CLI behavior: 0=success, 1=not_found, 2=error
